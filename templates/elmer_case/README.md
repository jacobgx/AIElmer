# {{CASE_ID}}

- 创建日期：{{DATE}}
- 当前状态：`DRAFT`
- 当前闸门：`G0 需求与证据接收`
- Elmer 版本：待记录

## 目标

在此用一句话写清求解目标和最终需要交付的量。

## 推荐顺序

1. 填写 `model_spec.yaml` 和 `uncertainties.md`。
2. 完成 `formulation.md`，确认公式、单位和边界。
3. 在 `syntax_evidence.md` 留下关键 SIF 语法证据。
4. 生成并核验网格及边界编号。
5. 从 `case.sif.template` 创建最小可运行 SIF。
6. 按 `validation_plan.md` 先跑 MVP，再跑正式模型。

总规范：`knowledge/ELMER_AI_MODELING_STANDARD.md`。

